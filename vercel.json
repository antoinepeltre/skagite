{
    "buildCommand": "npm run build",
    "outputDirectory": "dist/skagite",
    "framework": "angular",
    "routes": [
        { "src": "/assets/(.*)", "headers": { "cache-control": "v-max-age=31536000, immutable" } },
        { "src": "/(.*)", "dest": "/index.html" }
    ]
}